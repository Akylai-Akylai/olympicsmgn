import requests
response = requests.get('http://127.0.0.1:5000/products')
print(response.text)

***

from flask import Flask
app = Flask('')

products = ['картошка','редька','лук']

@app.route('/products')
def products():
  return products

app.run(debug=True)
 
